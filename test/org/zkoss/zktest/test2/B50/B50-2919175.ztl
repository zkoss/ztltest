<test tags="B50-2919175.zul,Combobox,Constraint">
	<case id="closed">
		<server>
			<!--
<zk>
1. select Cool!
<separator/>
2. type abc into combobox
<separator/>
3. tab away
<separator/>
4. It should not complain "This field should not be empty ...."
<combobox constraint="no empty">
<comboitem label="Simple and Rich" />
<comboitem label="Cool!" />
<comboitem label="Thumbs Up!" />
</combobox>
</zk>
			-->
		</server>
		<client>	<![CDATA[
		             				click(jq("@combobox .z-combobox-icon"));
		            				waitResponse();
		            				click(jq("@comboitem[label=\"Cool!\"] td.z-comboitem-text"));
		            				waitResponse();
		            				type(jq("@combobox input.z-combobox-input"), "abc");
		            				waitResponse();
		            				blur(jq("@combobox"));
		            				waitResponse();
		            				verifyFalse(jq(".z-errorbox").exists());
			
			]]></client>
	</case>
</test>