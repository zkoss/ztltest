<test tags="timebox,Unstable">
	<case id="type">
		<server><!--
			<zk>
				You should not type non-number character inside the input element(Opera only)
				<timebox/>
			</zk>
		 --></server>
		<client><![CDATA[
				focus(jq("@timebox input.z-timebox-input"));
				String old = jq("@timebox input.z-timebox-input").val();
				keyPress(jq("@timebox input.z-timebox-input"), "A");
				keyPress(jq("@timebox input.z-timebox-input"), "X");
				waitResponse();
				verifyEquals(old, jq("@timebox input.z-timebox-input").val());
		]]></client>
	</case>
</test>